## What does this change?

## Why?

## How was it tested?

- [ ] `make test` passes
- [ ] `make lint` passes
- [ ] `make generate manifests` produces no diff (if API changes)
- [ ] e2e suite passes (behaviour changes)
